# Workflow Explanation

- full-recon.sh orchestrates the entire recon pipeline.

- Modules operate independently and can be reused separately.

- Each module produces structured output for later stages.

- The framework is modular, scalable, and repeatable.
